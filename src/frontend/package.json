{
  "name": "devsignal-studio-frontend",
  "private": true,
  "version": "0.1.0-demo",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:demo": "tsc -p tsconfig.demo.json && cp index.demo.html dist/index.html && cp src/styles.css dist/styles.css && mkdir -p dist/vendor && cp public/vendor/* dist/vendor/",
    "preview:demo": "node server.mjs"
  },
  "dependencies": {
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^6.1.1",
    "typescript": "^5.9.0",
    "vite": "^8.2.2"
  }
}
